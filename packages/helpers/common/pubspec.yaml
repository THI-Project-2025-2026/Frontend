name: common_helpers
description: Future stateless helper utilities shared across packages.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.9.2

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
