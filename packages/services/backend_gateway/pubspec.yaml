name: backend_gateway
description: WebSocket gateway connectivity and measurement session management for Sonalyze frontend.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.9.2
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^9.1.0
  equatable: ^2.0.5
  uuid: ^4.5.1
  web_socket_channel: ^2.4.0
  meta: ^1.11.0
  http: ^1.2.0
  crypto: ^3.0.3
  # Measurement session dependencies
  audioplayers: ^6.0.0
  path_provider: ^2.1.2
  recording_service:
    path: ../recording

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
