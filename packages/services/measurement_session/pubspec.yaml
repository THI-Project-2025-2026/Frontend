name: measurement_session
description: Coordinated acoustic measurement session service for Sonalyze.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.9.2
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^9.1.0
  equatable: ^2.0.5
  http: ^1.2.0
  path_provider: ^2.1.2
  recording_service:
    path: ../recording
  backend_gateway:
    path: ../backend_gateway
  audioplayers: ^6.0.0
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
